/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'standalone',
    images: {
        formats: ['image/webp'],
        domains: ['api.ok-dev.pp.ua'],
    }
};

module.exports = nextConfig;
